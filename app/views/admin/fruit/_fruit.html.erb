<tr>
	<td><%= fruit.id %></td>
	<td><%= link_to fruit.name, :action=> "show", :id => fruit %></td>
	<td><%= fruit.brief_introduction %></td>
	<td><%= fruit.fruit_type_id %></td>
	<td><%= fruit.inventory %></td>
	<td><%= link_to "编辑", :action=> "edit", :id => fruit %></td>
	<td><%= button_to "删除", { :action=> "destroy", :id=> fruit },
															:confirm => "是否要删除水果种类：#{fruit.name}" %></td>
</tr>